//Given an array of integers, return true if there are integers of 100 next
//to each other. Array is at least of length 2

public boolean scores100(int[] scores) {
  int index = 0;
  for (int i = 0; i < scores.length; i++) {
    if (scores[index] == 100 && scores[index + 1] == 100) return true;
    index++;
  } return false;
}

//note to self: why scores.length - 1 or - 2 as check condition? 
